#include "monty.h"

/**
* nop - really does nothing
* @stack: pointer to pointer to the top of the stack
* @line_number: line number of the line read
*
* Return: void
*/
void nop(__attribute__((unused))stack_t **stack,
     __attribute__((unused))unsigned int line_number)
{
	;
}
